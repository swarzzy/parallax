#pragma once
#include <iostream>
#include "../Common.h"
#include "MemoryManager.h"

//inline void* operator new(size_t size) {
//	prx::internal::MemoryManager::registerAlloc(size);
//	void* ptr = malloc(size);
//	return ptr;
//}
